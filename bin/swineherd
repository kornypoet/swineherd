#!/usr/bin/env ruby
require 'swineherd'
Settings.define :script_param,:default => {}
Settings.resolve!
raise "No file path provided" unless Settings.rest.first
script = Swineherd::Script.new(Settings.rest.first,Settings.script_param)
puts script.contents
script.run(Settings)
